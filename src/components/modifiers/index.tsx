import ReactModal from 'react-modal'
import styles from './styles.module.css'

const 

export const modifiers = () => {
    return(
        <ReactModal 
            isOpen = {true}
            onRequestClose={true}
    )
}